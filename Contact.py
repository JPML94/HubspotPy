
class Contact(object):

    def __init__(self):

        value = 'this is the current value, num or string?'
        history = 'an array containing the versions as object for each'
        num_versions = len(history)
